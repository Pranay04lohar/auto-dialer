# Minimal MVP Flow

## 🔄 Simplified Request Flow

### User types "Call to +91 9876543210"

```
┌─────────────┐
│   User      │
│  (Browser)  │
└──────┬──────┘
       │
       │ POST /ai_prompt
       │ { command: "Call to +91 9876543210" }
       │
       ▼
┌─────────────────────┐
│  HomeController     │
│  #ai_prompt          │
└──────┬──────────────┘
       │
       │ Calls AIPromptService.parse()
       │
       ▼
┌─────────────────────┐
│  AIPromptService    │
│  (Simple regex or   │
│   OpenAI API)       │
└──────┬──────────────┘
       │
       │ Returns: { phone: "+919876543210" }
       │
       ▼
┌─────────────────────┐
│  HomeController     │
│  Enqueues job       │
└──────┬──────────────┘
       │
       │ CallExecutionJob.perform_later(phone)
       │
       ▼
┌─────────────────────┐
│  ActiveJob          │
│  (:async adapter)   │
│  (In-memory queue)  │
└──────┬──────────────┘
       │
       │ Job executes
       │
       ▼
┌─────────────────────┐
│  CallExecutionJob   │
│  #perform           │
└──────┬──────────────┘
       │
       │ Calls TwilioCallService.call(phone)
       │
       ▼
┌─────────────────────┐
│  TwilioCallService  │
│  POST to Twilio API │
└──────┬──────────────┘
       │
       │ Returns: call_sid
       │
       ▼
┌─────────────────────┐
│  CallLogService     │
│  @@logs << {        │
│    phone: phone,    │
│    status: 'initiated',
│    call_sid: sid    │
│  }                  │
└──────┬──────────────┘
       │
       │ (In-memory storage)
       │
       ▼
┌─────────────────────┐
│  Twilio API         │
│  Initiates call     │
└──────┬──────────────┘
       │
       │ Call connects
       │
       ▼
┌─────────────────────┐
│  Twilio requests    │
│  TwiML: GET /twiml  │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  WebhooksController │
│  #twiml             │
│  Returns TwiML:     │
│  <Say>Hello...</Say>│
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Twilio plays       │
│  AI voice message   │
└──────┬──────────────┘
       │
       │ Call ends
       │
       ▼
┌─────────────────────┐
│  Twilio sends       │
│  status callback    │
│  POST /webhooks/status
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  WebhooksController │
│  #status            │
│  Updates CallLogService│
│  @@logs.find { ... }│
│  .update(status)    │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  User refreshes     │
│  page or auto-refresh│
│  Sees updated logs  │
└─────────────────────┘
```

---

## 📊 Data Flow (In-Memory)

```
┌─────────────────────────────────┐
│  CallLogService                │
│  (Class variable)              │
│                                 │
│  @@logs = [                     │
│    {                            │
│      phone: "+919876543210",   │
│      status: "completed",       │
│      call_sid: "CAxxxxx",       │
│      timestamp: Time.now        │
│    },                           │
│    { ... },                     │
│    ...                          │
│  ]                              │
│                                 │
│  Methods:                       │
│  - self.add(phone, status, sid) │
│  - self.all                     │
│  - self.stats                   │
└─────────────────────────────────┘
```

---

## 🎯 Key Simplifications

1. **No Database**: In-memory array (`@@logs`)
2. **No Redis**: ActiveJob with `:async` adapter
3. **Single Controller**: `HomeController` handles everything
4. **Simple Storage**: Ruby hash/array
5. **Basic UI**: Single page, no complex routing

---

## ⚡ Performance Notes

- **In-memory storage**: Fast, but resets on server restart
- **ActiveJob async**: Simple thread pool, good for MVP
- **No persistence**: Logs lost on restart (OK for MVP)
- **Simple stats**: Calculated on-the-fly from array

---

This is the minimal flow that solves the core problem! 🚀



